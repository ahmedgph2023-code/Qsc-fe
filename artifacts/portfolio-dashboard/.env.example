# Portfolio dashboard environment (Vite)
# Copy to .env and adjust per machine / deployment.

# Backend REST API (exposed to browser — must be VITE_ prefix)
VITE_API_BASE=http://localhost:5001/api

# Dev server (read by vite.config.ts at startup)
PORT=5173
BASE_PATH=/
