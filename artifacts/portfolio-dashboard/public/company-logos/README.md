# Company logos (portfolio statement)

Place one file per ticker, named exactly like the QSE symbol:

- `MHAR.svg` (preferred) or `MHAR.png`
- Source: QSC server Desktop pack (?-26) / client SVG pack

The UI loads `/company-logos/{TICKER}.svg`. If the file is missing, a letter badge is shown.
